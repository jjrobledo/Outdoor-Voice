export default function Navbar() {
  return (
    <div
      style={{
        padding: 20,
        fontFamily: "sans-serif",
        color: "#fff",
        backgroundColor: "#000",
      }}
    >
      <span>Outdoor Voice</span>
    </div>
  );
}
